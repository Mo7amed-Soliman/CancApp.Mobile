import 'package:flutter/material.dart';

class PsychiatristView extends StatelessWidget {
  const PsychiatristView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Psychiatrist View'),
      ),
    );
  }
}
