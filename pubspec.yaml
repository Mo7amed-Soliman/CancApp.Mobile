name: canc_app
description: "CancApp is a Flutter-based mobile application aimed at enhancing the quality of life for cancer patients"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.6.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  # This Library defines the DateFormat and NumberFormat
  intl: ^0.19.0
  # This Library used to support multiple languages
  flutter_localizations:
    sdk: flutter
  # flutter_native_splash for splash screen management
  flutter_native_splash: ^2.4.4
  # device_preview for responsive design testing
  device_preview: ^1.2.0
  # go_router for navigation management
  go_router: ^14.8.0
  # state management
  flutter_bloc: ^9.1.0
  # dependency injection
  get_it: ^8.0.3
  # flutter_svg for svg images
  flutter_svg: ^2.0.17
  # Pin code input (OTP) text field  
  pinput: ^5.0.1
  # iconly for icons management   
  iconly: ^1.0.1
  # bot_toast for toast messages
  bot_toast: ^4.1.3
  # persistent bottom navigation bar
  persistent_bottom_nav_bar: ^6.2.1
  # image picker for selecting images from gallery or camera
  image_picker: ^1.0.7
  # equatable is a package that helps you simplify and standardize equality comparisons between objects
  equatable: ^2.0.5
  # uuid for unique id generation
  uuid: ^4.3.3
  # easy_date_timeline for date timeline management
  easy_date_timeline: ^2.0.9
  # redacted is a package that helps Shimmer effect
  redacted: ^1.0.13
  # Hive for local storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  # persistent key-value storage for simple data
  shared_preferences: ^2.5.2
  # flutter_secure_storage for secure storage of tokens
  flutter_secure_storage: ^9.2.4
  # flutter_dotenv for environment variables (API_URL, API_KEY, etc.)
  flutter_dotenv: ^5.2.1
  # path_provider for accessing device file system
  path_provider: ^2.1.2
  # local notifications 
  flutter_local_notifications: ^19.1.0
  # timezone for timezone management
  timezone: ^0.10.0
  # managing app permissions
  permission_handler: ^11.3.1
  # uesd in the return type of the methods
  dartz: ^0.10.1
  # dio for http requests
  dio: ^5.7.0
  # plugin for easy access to device's location in real-time.
  location: ^8.0.0

 

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  # flutter_launcher_icons for generating app icons 
  flutter_launcher_icons: "^0.13.1"
  # Hive generator for type adapters
  build_runner: ^2.4.8
  hive_generator: ^2.0.1


# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/on_boarding/
    - assets/icons/
    - assets/images/dummy_image/
    - .env
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
        - asset: assets/fonts/Poppins-Medium.ttf
          weight: 500
        - asset: assets/fonts/Poppins-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Poppins-Bold.ttf
          weight: 700  

    - family: Almarai
      fonts:
        - asset: assets/fonts/Almarai-Regular.ttf
        - asset: assets/fonts/Almarai-Bold.ttf
          weight: 700

    - family: Righteous
      fonts:
        - asset: assets/fonts/Righteous-Regular.ttf
 
flutter_intl: 
  enabled: true